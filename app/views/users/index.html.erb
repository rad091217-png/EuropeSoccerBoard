<%= stylesheet_link_tag "users_index", media: "all", 'data-turbolinks-track': "reload" %>
<h1 class="all_users">全てのユーザー</h1>
<%= will_paginate %>
<ul class="users">
    <% @users.each do |user| %>
    <%= render user %>
    <% end %>
</ul>

<%= will_paginate %>