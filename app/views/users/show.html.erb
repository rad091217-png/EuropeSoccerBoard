<%= stylesheet_link_tag "users_show", media: 'all', 'data-turbolinks-track': 'reload' %>
    <h1 class="user_name">
        <%= gravatar_for @user %>
        <%= @user.name %>
    </h1>
    <%= link_to "DM", "rooms/show" %>
    <section class="stats">
      <%= render 'shared/stats' %>
    </section>
    <%= render 'follow_form' if logged_in? %>
