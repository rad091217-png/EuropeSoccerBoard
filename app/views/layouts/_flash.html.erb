<% if flash.now[:success] %>
      <div class="flash time-limit-success">
        <%= flash.now[:success] %>
        </div>
        <% else %>
        <div class="flash time-limit-danger">
        <%= flash.now[:danger] %>
      </div>
    <% end %>